# homebrew-gmr

Homebrew tap for [gmr](https://github.com/gmr)'s tools.

```bash
brew install gmr/gmr/pgfmt          # PostgreSQL SQL formatter
brew install gmr/gmr/pglifecycle    # PostgreSQL schema management
```

Formulas are updated automatically by each project's release workflow.
